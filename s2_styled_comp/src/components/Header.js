import React from "react"
import styled from "styled-components"

export default function Header() {
  const StyledHeader = styled.h1`
    color: red;
  `
  return <StyledHeader>Console on steroids</StyledHeader>
}
