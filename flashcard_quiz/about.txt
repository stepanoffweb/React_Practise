How To Build A Flashcard Quiz With React
https://www.youtube.com/watch?v=hEtZ040fsD8&t=130s
----------
* Работа с API (Trivia knowledge DB)
* Работа с хуками (useRef, useEffect, useState)
* Grid-позиционирование
* Вывод полученнного с сервера текста в читабельной форме ( без &gt;&lt; ...)
----------
> npx create-react-app . // точка = проект в текущей папке
- оцениваем необходимые компоненты для отображения фейса
- создаем моковые базы данных (CONST) в App дл подгонки стилей и рендеринга презентационных компонентов 
- пользуясь useState() формируем начальное состо¤ние приложения, убеждаясь  в работоспособности модели
в том числе стили компонентов и их смену по событиям (onClick={() => setFlip(!flip)} + динамические классы)
- максимально стилизуем моковый шаблон в файле src/app.css и импортим его в родительский компонент App (index - ???)
!!! использование переменных в стилях,  transform 3D
- opentdb.com - API 'open Trivia DB' формирует форму опросника с вариантами ответов. Ключ не нужен
- npm i axios
- useEffect
- избавляемся от  &lt;slide&gt;&lt;/slide&gt; (function decode(str){textArea.innerHTML = str;
    return textArea.value;} ) !!! создаем элемент, но не вставляем его в DOM
- позиционируем гридами 
.card-grid {
    display: grid;    
    align-items: center;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1rem;
}
- ставим рефы на повторяющиеся элементы компонентов: сonst frontEl, backEl = useRef() // frontEl.current -? , вычисляем высоту
элементов и передаем значение динамически в инлайн-стили c useEffect
- добавляем useEffect с обработчиком ресайза окна - пересчет и перерисовка при изменении высоты экрана
- создаем форму для запросов (количество карточек с вопросами, тема и сложность вопросов, тип ответа - выбор/ тру-фолс)
используя useRef, useEffect, useState

===== Qs
- после задания переменной transform: rotateY она не сработала ниже в .card .back... пришлось оставить по старинке.
- !!! строит вложенность на флексах и одновременно дает родителю - релатив, а детям - абсолют с паддингами????? чозанах